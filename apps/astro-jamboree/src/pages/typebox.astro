---
const isIframe = Astro.url.searchParams.has('iframe');
import Layout from '../layouts/Layout.astro';
import { TypeboxPage } from '../app/app/parsers/pages/typebox';
---

<Layout title="Typebox validator feature tests">
  <TypeboxPage client:only="react" isIframe={isIframe} />
</Layout>
