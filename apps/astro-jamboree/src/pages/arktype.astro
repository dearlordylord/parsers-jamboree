---
const isIframe = Astro.url.searchParams.has('iframe');
import Layout from '../layouts/Layout.astro';
import { ArktypePage } from '../app/app/parsers/pages/arktype';
---

<Layout title="Arktype validator feature tests">
  <ArktypePage client:only="react" isIframe={isIframe} />
</Layout>
