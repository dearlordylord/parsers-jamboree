---
const isIframe = Astro.url.searchParams.has('iframe');
import Layout from '../layouts/Layout.astro';
import { RescriptSchemaPage } from '../app/app/parsers/pages/rescript-schema';
---

<Layout title="Rescript-schema validator feature tests">
  <RescriptSchemaPage client:only="react" isIframe={isIframe} />
</Layout>
